minimum_pre_commit_version: '4.0.0'

repos:
  - repo: local
    hooks:
      - id: helixiora-quality
        name: helixiora-quality
        entry: make pre-commit
        language: system
        pass_filenames: false
        always_run: true
